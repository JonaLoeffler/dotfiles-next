if [ -d "$HOME/.cargo/env" ]; then
  . "$HOME/.cargo/env"
fi

export XDG_CONFIG_HOME=$HOME/.config

export VISUAL=nvim
export EDITOR=nvim
alias vim=nvim

export GTK_THEME=Adwaita-dark

export XDG_CONFIG_HOME="$HOME/.config"
